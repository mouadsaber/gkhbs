# Order email (destination)
ORDER_EMAIL=contact@gkhbs.com

# SMTP config for Nodemailer (/api/order)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Optional: From address (defaults to SMTP_USER)
SMTP_FROM=

